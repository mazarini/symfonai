{% extends "base.html.twig" %}

{% block title %}
  SymfonAI — Chat
{% endblock %}

{% block body %}
  <main class="container" style="max-width: 700px; margin: 2rem auto;">
    <h1>
      💬 SymfonAI
    </h1>
    <p class="text-muted">
      Pose une question et laisse l’IA Symfony te répondre.
    </p>

    <form method="post" action="{{ path("symfonai_chat") }}" class="mb-4">
      <div class="mb-3">
        <label for="question" class="form-label">Ta question :</label>
        <textarea name="question" id="question" rows="3" class="form-control" required>
          {{ question ?? "" }}
        </textarea>
      </div>

      <button type="submit" class="btn btn-primary">Envoyer</button>
    </form>

    {% if answer is not null %}
      <div class="card mt-4">
        <div class="card-header bg-light">
          <strong>Réponse :</strong>
        </div>
        <div class="card-body">
          <p>
            {{ answer }}
          </p>
        </div>
      </div>
    {% endif %}
  </main>
{% endblock %}
