<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>
      {% block title %}
        SymfonAI
      {% endblock %}
    </title>

    {% block stylesheets %}
      {# CSS global (Bootstrap + app.scss compilé) #}
      {{ encore_entry_link_tags("app") }}
    {% endblock %}
  </head>
  <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light mb-4">
      <div class="container">
        <a class="navbar-brand" href="{{ path("symfonai_chat") }}">SymfonAI</a>
      </div>
    </nav>

    <div class="container">
      {% block body %}
        {# Contenu principal #}
      {% endblock %}
    </div>

    {% block javascripts %}
      {# JS global (Bootstrap + app.js compilé) #}
      {{ encore_entry_script_tags("app") }}
    {% endblock %}
  </body>
</html>
